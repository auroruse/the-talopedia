{
  "name": "talopedia",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --port 4321",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "retcon": "node scripts/retcon.mjs",
    "sync-coords": "cp \"../Avium Map/src/data/coordinates.tsv\" src/content/data/coordinates.tsv",
    "clean": "rm -rf node_modules/.astro node_modules/.vite .astro dist",
    "sync": "node scripts/sync.mjs",
    "sync-pickem": "node scripts/sync-pickem.mjs"
  },
  "dependencies": {
    "astro": "^5.1.1",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
